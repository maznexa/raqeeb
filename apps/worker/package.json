{
  "name": "@raqeeb/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Raqeeb background worker — outbox publisher + BullMQ queues",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm build && node dist/main.js",
    "start": "node dist/main.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "true"
  },
  "dependencies": {
    "@raqeeb/db": "workspace:*",
    "bullmq": "^5.34.8",
    "drizzle-orm": "^0.38.4",
    "ioredis": "^5.4.2",
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
